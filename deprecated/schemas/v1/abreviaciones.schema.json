{
  "$id": "https://sebascaules.github.io/Cuatris/schemas/v1/abreviaciones.schema.json",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "description": "Nombre corto curado a mano con el que se conoce cada materia.",
  "properties": {
    "abreviaciones": {
      "additionalProperties": false,
      "description": "Mapa de codigo de materia a su abreviacion; las abreviaciones son unicas.",
      "patternProperties": {
        "^\\d{2}\\.\\d{2}$": {
          "description": "Abreviacion de la materia, de 1 a 24 caracteres.",
          "maxLength": 24,
          "minLength": 1,
          "type": "string"
        }
      },
      "type": "object"
    },
    "contrato": {
      "description": "Version del contrato de datos, en SemVer.",
      "pattern": "^\\d+\\.\\d+\\.\\d+$",
      "type": "string"
    }
  },
  "required": [
    "contrato",
    "abreviaciones"
  ],
  "title": "Abreviaciones de materias",
  "type": "object"
}
