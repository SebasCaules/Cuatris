# material crudo: nunca entra al repositorio
material-raw/

# entornos y caches
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
node_modules/
app/dist/
app/.vite/
*.log

# sistema
.DS_Store

# artefactos de empaquetado y caches del scraper
*.egg-info/
.cuatris-cache/

# salida de `cuatris pr triage --salida triage.json`: la genera el workflow en el runner
# (.github/workflows/pr-datos.yml) y tambien queda al correrlo a mano; nunca se versiona
triage.json

# cache del hook de diseño (la configuración sí se versiona)
.impeccable/hook.cache.json
