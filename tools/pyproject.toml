[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "cuatris"
version = "0.2.0"
description = "Scraper de horarios del SGA del ITBA para Cuatris (contrato 1.1.0)"
requires-python = ">=3.11"
license = { text = "MIT" }
dependencies = [
  "beautifulsoup4>=4.12",
  "httpx>=0.27",
]

[project.optional-dependencies]
dev = ["pytest>=8", "ruff==0.16.7"]

[project.scripts]
cuatris = "cuatris.cli:main"

[tool.setuptools.packages.find]
where = ["."]
include = ["cuatris*"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "B", "UP"]

[tool.pytest.ini_options]
testpaths = ["tests"]
